services:
  bot:
    build: .
    container_name: ${PROJECT_NAME?PROJECT_NAME must be set}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - NODE_ENV=production
    labels:
      - "traefik.enable=${TRAEFIK_ENABLE:-false}"
      - "traefik.docker.network=${TRAEFIK_NETWORK?TRAEFIK_NETWORK must be set}"
      - "traefik.http.routers.${PROJECT_NAME}-bot.rule=Host(`${DOMAIN_NAME:-}`)"
      - "traefik.http.routers.${PROJECT_NAME}-bot.entrypoints=${TRAEFIK_ENTRYPOINT:-websecure}"
      - "traefik.http.routers.${PROJECT_NAME}-bot.tls=true"
      - "traefik.http.services.${PROJECT_NAME}-bot.loadbalancer.server.port=3000"
    networks:
      - proxy-public

networks:
  proxy-public:
    external: true
    name: ${TRAEFIK_NETWORK?TRAEFIK_NETWORK must be set}
