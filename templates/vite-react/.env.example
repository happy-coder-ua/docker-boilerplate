# Local development: copy to .env
# Production: do NOT commit .env; CI/CD creates it on the server from CI variables/secrets

# PROJECT_NAME: Used for container name and Traefik router/service naming.
# Example: my-vite-react-project
PROJECT_NAME=my-vite-react-project

# DOMAIN_NAME: Public hostname routed by Traefik.
# Example: app.example.com
DOMAIN_NAME=example.com

# TRAEFIK_NETWORK: External Docker network name that Traefik and this service share.
# Default: proxy-public
TRAEFIK_NETWORK=proxy-public

# TRAEFIK_ENTRYPOINT: Traefik entrypoint name (depends on your Traefik config).
# Examples: https, websecure
# Default in docker-compose labels: https
TRAEFIK_ENTRYPOINT=https
