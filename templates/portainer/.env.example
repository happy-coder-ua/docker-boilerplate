# Local development: copy to .env
# Production: do NOT commit .env; CI/CD creates it on the server from Secrets/Variables

PROJECT_NAME=portainer
DOMAIN_NAME=portainer.example.com
TRAEFIK_NETWORK=proxy-public

# Optional: extra protection in Traefik via Basic Auth.
# Used only if you create docker-compose.override.yml (install.sh can generate it).
# Example value format: "user:$2y$05$..." (htpasswd bcrypt)
# PORTAINER_BASIC_AUTH_USERS=
